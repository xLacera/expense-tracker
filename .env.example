# ============================================
# Expense Tracker - Variables de Entorno (ejemplo)
# Copia este archivo como .env y llena los valores
# ============================================

# === DESARROLLO LOCAL (Docker Compose) ===

# PostgreSQL
POSTGRES_USER=expense_user
POSTGRES_PASSWORD=cambia_esta_password
POSTGRES_DB=expense_tracker
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Backend
BACKEND_PORT=8080
JWT_SECRET=cambia-este-secreto-jwt
GIN_MODE=debug

# Frontend
VITE_API_URL=http://localhost:8080/api

# === PRODUCCIÓN (Render + Neon + Vercel) ===
# En producción, usa DATABASE_URL en vez de las variables individuales de PostgreSQL.
# DATABASE_URL=postgres://usuario:password@host/basededatos?sslmode=require
# CORS_ORIGIN=https://tu-app.vercel.app
# GIN_MODE=release
# Resend (restablecer contraseña por email). Obtén la API Key en https://resend.com
# RESEND_API_KEY=re_xxxxxxxxxxxx
